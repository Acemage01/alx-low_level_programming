#include <stdio.h>

/**
 * main - prints all possible combinations of single-digit numbers
 * Return: 0 Always
 */

int main(void)
{
	int d;

	for (d = 48; d < 58; d++)
	{
		putchar(n);
		if (d != 57)
		{
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}
